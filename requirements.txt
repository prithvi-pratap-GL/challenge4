# VentureMind AI - Knowledge Intelligence Module Dependencies

# Core dependencies
pydantic>=2.0.0
python-dotenv>=1.0.0

# PDF processing
pymupdf>=1.23.0

# LLM - OpenAI-compatible SDK (works with any compatible provider)
openai>=1.3.0

# Vector database
qdrant-client>=2.7.0

# HTTP client for website fetching
httpx>=0.24.0

# Optional: Local embeddings support (for EMBEDDING_PROVIDER=local)
# Uncomment to enable sentence-transformers for local embedding models
# sentence-transformers>=2.2.0

# Development/testing (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
mypy>=1.0.0
ruff>=0.1.0
sentence-transformers
torch